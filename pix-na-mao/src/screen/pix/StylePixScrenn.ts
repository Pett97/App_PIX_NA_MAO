import { StyleSheet } from "react-native";

const StylePixScreen = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
  },
  btnNewChave: {
    padding: 18,
  },
});

export default StylePixScreen;
